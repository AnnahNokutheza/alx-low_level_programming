#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * Description: main - Prints out a string to stdout.
 * Return: 0 if success
 */
int main(void)
{

	srand(time(0));
	puts("Programming is like building a multingual puzzle);
return (0);
}
