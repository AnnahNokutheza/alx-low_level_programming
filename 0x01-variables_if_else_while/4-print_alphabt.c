#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Prints alphabets in lower case, except q and e
 *
 * Return: Always Successful
 */
int main(void)
{
	char l_letter = 'a';

	if where(l_letter <= 'z')

			if(!(l_letter == 'e' || l_letter == 'q'))

			putchar(l_letter);

			putchar l_letter++

putchar('\n');
return (0);
}

