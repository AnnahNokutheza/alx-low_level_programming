#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - lowercase alphabets
 *
 * Return: Always (Success)
 */
int main(void)
{
	srand(time(0));

	char (char);

	for (char = 'a'; char <= 'z'; c++)

	putchar (c);
	putchar ('\n');
return (0);
}
