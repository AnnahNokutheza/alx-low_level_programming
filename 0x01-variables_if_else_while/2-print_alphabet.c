#include <stdio.h>

/**
 * main - print if the number is positive, zero, or negative
 *
 * Return: Alaways 0 (Success)
 */
int main(void)
{
	char low;
	for (low = 'a' ; low <= 'z' ; low++)

		        putchar(ch);
	                putchar("\n");
	
	return (0);
}
